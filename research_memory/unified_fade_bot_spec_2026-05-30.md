# 🧊 SPEC GELÉE — BOT FADE UNIFIÉ (mean-reversion tout-terrain) (2026-05-30)

## ⚡ ÉTAT FINAL (TL;DR — lire CECI d'abord ; le reste = log chronologique avec des verdicts intermédiaires RENVERSÉS)

**LE BOT DÉPLOYABLE (in-sample, pending OOS 11 juin) :**
> À chaque fin de jambe zig-zag **50bps**, en **régime tendanciel** (drift_4h tercile bas=bear / haut=bull, chop exclu) :
> 1. **FADE l'extension** (bear: down-leg→long le dip ; bull: up-leg→short le rip + filtre accel-1h).
> 2. **Filtre FLUX** : SKIP si `sign(tfi_5min)==jambe & |tfi_5min|≥Q70` (flux taker soutient la jambe = toxique).
> 3. **Filtre BINANCE-LEAD** : SKIP si `|drift Binance 120s| ≥ 30bps & sign==jambe` (le marché leader court déjà = continuation informée).
> 4. **Filtre TREND-PERSISTANT (drift_8h)** : skip si la jambe étend un trend 8h fort (`sign(drift_8h)==jambe & |drift_8h|≥Q70`) = ne pas fader la continuation d'un trend persistant → **c'est le détecteur choppy/trend, qui gère HYPE DYNAMIQUEMENT (HYPE −8.1→+2.7, majors intacts), SANS hardcode.**
> 5. **Hold 30min, sortie MAKER, PAS de stop dur, sizing petit.** Univers = perps liquides ; **HYPE NON hardcodé-exclu** (fadé quand il choppe, skippé quand il trend, via le gate drift_8h). (Mode OFFENSIF Binance-extrême→FOLLOW = **TESTÉ & RÉFUTÉ** 2026-06-01, cf bullet ✗ ci-dessous ; Binance reste **DÉFENSIF only**.)

**PERF in-sample (fills réels + les 2 filtres) :** net **~+11 bps/trade**, worst **−71/−101** (vs −238 sans Binance-lead), **maxDD ÷2**, ~13+ trades/j, cross-week (W507+W516) + sur les 2 régimes.

**CE QUI A ÉTÉ RENVERSÉ EN COURS DE ROUTE (ne PAS croire les vieux bullets ci-dessous qui disent le contraire) :**
- ✅ **La queue short-gamma est RÉDUCTIBLE** via le filtre Binance-lead (qui AMÉLIORE l'EV en plus) — PAS "irréductible/size-for-it" comme dit avant.
- ✅ **Stop dur = CONTRE-PRODUCTIF** (augmente le maxDD), on n'en met pas.
- ✅ **Momentum / HYPE-trend = BÉTA pas alpha** (pire que buy&hold) → sleeve momentum MORTE, bot = fade SEUL.
- ✅ **Pas de drop-ETH statique** (period-fitting) ; pas de "tokens momentum à collecter" (rien à timer).
- ✅ **Détecteurs d'ÉTAT single-venue** (OFI/thinning/breadth/stop) = échouent ; seuls **FLUX (Ahern) + BINANCE-LEAD (cross-venue) + drift_8h (trend-persistant)** marchent.
- ✗ **Mode OFFENSIF Binance-extrême (FOLLOW) = RÉFUTÉ** (`_fade_binlead_offensive_hl.py` + `_fade_binlead_offensive_fast_hl.py`, 2026-06-01, le DERNIER bloc du bot pas encore mesuré) : suivre un move violent Binance en **TAKER** sur HL ne paie PAS. La continuation HL dans le sens du move Binance est **sub-1 bps à TOUS les horizons** (gross culmine +0.9bps @2-5s puis décay négatif ; hit 44-52% = pile/face) sur **n>10 000 events**, **JAMAIS > 2.5bps (1 jambe taker) ni > 6 (RT taker)**, sur BTC/ETH/SOL/XRP/**HYPE** et tous seuils (THR 30-80bps, W 30-120s, H 2s-30min). HL est déjà arbitré sur Binance à la seconde → **rien à chasser au timescale du user** (qui ne court PAS la ms ; l'edge net-flow 2-5s de mai-19 exigeait latence 20-100ms + était <2.5bps de toute façon, donc inaccessible en taker). **Implication : l'EXIT-contraire mid-trade ne sauverait rien non plus** (continuation≈0 → ça dégénère en stop dur, déjà montré contre-productif). ⇒ **Binance-lead = DÉFENSIF/SÉLECTION à l'entrée du fade UNIQUEMENT** (skip la jambe informée : worst −238→−71), PAS une entrée offensive. Le bot ne perd rien (il n'avait pas ce sleeve) — il ne gagne juste pas de nouveau sleeve.
- ✅ **DÉTECTEUR CHOPPY-vs-TREND TROUVÉ = drift_8h** (`_fade_longtrend_gate_hl.py`) : l'intuition "skew" du user, au bon timescale (multi-heures, PAS l'ER intraday qui a été RÉFUTÉ). HYPE |drift_8h|=382bps vs majors 82-124 → discrimine. Fade qui étend un trend_8h fort = −14.6 vs reste +5.2. Gate skip ces uns → **HYPE −8.1→+2.7 (skip ses 49% trending), majors quasi intacts (BTC 100% gardé)** = gère HYPE DYNAMIQUEMENT sans hardcode. (Q70 poolé → live = roulant par actif.) **= le chaînon "aware sans catégorisation", trouvé.**
  - **NUANCE métrique (`_fade_er_windows_hl.py`, user a demandé ER glissant 8h/24h) : c'est le NET DRIFT brut qui marche, PAS l'Efficiency Ratio.** ER glissant (4h/8h/24h par paire) testé = RÉFUTÉ : **HYPE a l'ER le plus BAS à toutes les fenêtres** (0.019→0.007, baisse avec W) car HYPE oscille tellement (path énorme) que son drift est une petite fraction → ER bas. **ER confond "oscillatoire" avec "choppy" et rate le drift lent.** Donc détecteur = **|drift_8h| (net move) avec seuil roulant PAR ACTIF** (percentile sur l'historique de chaque paire = scale-free, glissant, par-paire — le bon design du user, sur le bon métrique).
  - **VERSION LIVE = percentile ROULANT PAR ACTIF, causal (`_fade_drift8h_rolling_live_hl.py`, 2026-06-01)** : ring-buffer |drift_8h| 5min / fenêtre glissante 3j / chaque paire vs SON propre historique (remplace le Q70 poolé full-sample qui était lookahead+cross-asset). **Rescue HYPE confirmé CAUSAL : −8.1 → −0.3 (P70, n=371, 89% warmed) ; trade-weighted le livre AVEC HYPE passe ~−0.2 → +2.7 bps/trade.** **MAIS découverte clé : un gate per-asset EN AVEUGLE trime les majors qui, eux, mean-revertent même en extension** (XRP +9.3→+0.4, SOL +10.7→+6.9 ; BTC/ETH montent). **C'est l'inverse de HYPE : les extensions de XRP/majors REVERTENT (= excellents fades, NE PAS skip) ; celles de HYPE CONTINUENT (= toxiques, skip).** ⇒ le gate n'est REQUIS que si HYPE (ou un actif persistant) est dans le livre ; sur majors-seuls il nuit légèrement. **RAFFINEMENT propre (à tester) : n'activer le skip que pour les paires DIRECTIONNELLEMENT PERSISTANTES** — mesure depuis le MÊME buffer : `frac(drift_8h>0)` HYPE=**0.66** (persistant) vs majors **0.48-0.55** (flippy/choppy). Ça rescue HYPE SANS trimer les majors. **OBSERVABILITÉ livrée** (ce que le user veut surveiller) : par paire, évolution jour-par-jour (med|d8|, dir signé, seuil-roulant, %trend) — HYPE med|d8|=147bps & seuil 113→453 (s'adapte quand il chauffe) vs majors 40-62bps & seuil 50-120 = **scale-free par-actif visible à l'œil ; dir HYPE persistant-haussier, majors oscillent autour de 0**.

**CAVEATS DURS :** tout est **in-sample mai 2 semaines** ; seuils (Q70, 30bps/120s) fittés ; **Binance = SIGNAL only** (exec interdite MiCA → feed WS temps-réel requis en live, latence OK car lead 1-2min) ; mid Binance reconstruit. **LE JUGE = OOS 11 juin** (carnet HL frais ; juge fade+FLUX+Binance-lead cross-période). **NE PAS sizer avant.**

**CE QUI RESTE :** (1) OOS 11 juin ; (2) re-chiffrer le sizing avec la queue réduite (−71/−101) → nouveau rendement déployable (probablement > ~4-6%/mois) ; (3) infra : feed Binance WS sur VM ; (4) plus tard, si fade confirmé : couche news (catastrophe + béta directionnel) + collecte Binance ticks/L2.

---

**Statut : CANDIDATE LA PLUS SOLIDE DU PROJET, gelée. A clôturé signal + décompo + null + cross-week (données propres) + exécution réelle (carnet L2) + les DEUX régimes. PENDING = juge cross-PÉRIODE (carnet HL frais 11 juin). NE PAS sizer avant le 11 juin.**

Réalise la vision user "tout-terrain direction-agnostic" — MAIS pas par une bascule fade/ride : **par le FADE seul, qui marche en bull ET bear.** (Le RIDE/momentum a été testé = PAS de complément robuste sur majors, cf §ÉCARTÉ.) Fusion des anciens S1 (bear) + S2 (bull-accel).

---

## LA RÈGLE UNIFIÉE (figée)

- **Univers** : **BTC, SOL, XRP** (HL perps USDC). **ETH À EXCLURE** (`_fade_freq_tail_hl.py`, 2026-05-30) : ETH = driver de TOUTE la queue — les **4 pires trades du livre sont TOUS ETH** (−238/−114/−106/−101) ; **sans ETH worst −92, ZÉRO trade < −100**, mean/trade +12.2 (vs +9.6 avec ETH). ETH aussi le plus faible/flaky. Drop ETH → queue douce ET (sizing fixé par le worst) **~2.6× plus de notionnel à risque égal ⇒ ~2.6× le rendement.** **HYPE EXCLU** (actif-momentum → fade catastrophique, bull −35 NET/SIG).
- **FRÉQUENCE (`_fade_freq_tail_hl.py`)** : ~**13 trades/jour** (BTC/SOL/XRP ; médiane 12, min 1, max 33) — tire EN GRAPPES (choppy=mitraille, trend=silence). Pertes < −50 ≈ 1.4/j (banal) ; sans ETH **aucune < −100**. **Les pires pertes sont CORRÉLÉES (même minute, plusieurs paires : 17mai 23:40 ETH−238+XRP−92 ; 04mai ETH−114+XRP−82)** = événement de marché qui frappe N fades à la fois → le vrai tail à sizer = cluster corrélé, pas trade isolé.
- **Détecteur de jambe** : zig-zag first-passage **50 bps** sur le vrai mid L2 (fin de jambe B_i + direction d_i à chaque franchissement ±50bps depuis le dernier pivot).
- **Gate régime** : `drift_4h`. Tercile BAS = **bear** (downtrend), tercile HAUT = **bull** (uptrend), milieu = **chop EXCLU**. macro_sign = sign(drift_4h). Seuils terciles roulants/actif, recalibrés bi-hebdo.
- **Entrée (fade l'EXTENSION du macro)** :
  - **BEAR** : jambe = down-leg (d == macro_sign) → **FADE = LONG** (buy the dip). *Toutes* les extensions.
  - **BULL** : jambe = up-leg (d == macro_sign) **ET filtre accel-1h** (`drift_1h ≥ médiane(d1 dans bull)`) → **FADE = SHORT** (short the rip). Le filtre accel est REQUIS (bull brut instable cross-week).
  - Poster un ordre **MAKER au best** (bid si long / ask si short). Fill réel ~90% (spreads HL ~0.35bps → fill près du mid, adverse-selection PETITE, mesurée).
- **Hold** : **30 min** (robuste cross-week ; ×4 capital-turnover vs 2h). 
- **Sortie** : **MAKER** (on travaille la limite, hold = minutes, pas pressé) → RT ~3 bps.
- **Déploiement** : comme un **LIVRE DIVERSIFIÉ** sur BTC/SOL/XRP (la robustesse est portefeuille, PAS par-paire — cf caveats).

## PERF (in-sample mai, fills CARNET RÉELS, `_fade_unified_validate_hl.py`)

- **MAJORS POOLÉS — bear ET bull + sur les 2 semaines** : bear NET/SIG +19.0 (W507) / +3.2 (W516) ; bull +9.4 / +3.2. fill ~90%, win 51-71%.
- Par paire : **SOL** propre des 2 côtés × 2 sem ; **BTC/XRP** bear robuste / bull shaky ; **ETH** bull robuste / bear flippe (W516 −16.6) ; HYPE exclu.
- bps/heure ≈ +18 (hold 30min). ~10 trades/j à 50bps sur 3 majors (plus si seuil de jambe abaissé à 20-30bps).

## CHAÎNE DE PREUVE (gates passés)

1. **Signal réel (décompo `_oscillation_decompose.py`)** : vraie mean-reversion (fade l'extension qui sur-étend le trend 4h → retrace), PAS du trend-following.
2. **HL robuste in-sample** (`_oscillation_hl_robust.py`) : median ≈ mean, hit net-de-fee 58-71% (bear), pas porté par outliers.
3. **Cross-week DONNÉES PROPRES** (`_fade_crossweek_hl.py`) : bear + sur W507 ET W516 (BTC/SOL/XRP).
4. **Exécution réelle** (`_fade_maker_fill_hl.py`, `_fade_maker_fill_short_hl.py`, `_fade_unified_validate_hl.py`) : survit aux vrais fills maker carnet L2 (~90% fill), hold court.
5. **OOS Binance conservateur** (`_oscillation_oos_robust.py`, `_fade_exit_sim.py`) : direction bear confirmée (l'artefact de-bounce joue CONTRE le fade → positif = borne basse).
6. **Null-contrôlé** : NULL (signe aléatoire) ≈ −fee ; le fade le bat de ~+10 (bear), SHARP-bull +7.2 vs NULL −3.5.

## CE QUI A ÉTÉ ÉCARTÉ (résultats négatifs qui FAÇONNENT la spec)

- **TP serré (tp7sl10/10sl15/7sl20) = NÉGATIF partout** (in-sample ET OOS) → PAS de TP serré, tenir au temps.
- **hold-2h = inefficient** (+2.5 bps/h) → 30min (+18 bps/h) ; même bps/trade en ¼ du temps.
- **bull BRUT = instable cross-week** → filtre **accel-1h** requis (mais accel n'aide PAS le bear, qui marche sur toutes extensions).
- **RIDE / momentum (`_ride_continuation_hl.py`)** : ride-extension = −13 majors (mort) ; continuation buy-pullback = ~null. **Le ride n'ajoute RIEN sur majors** → all-terrain = fade-dans-2-régimes, PAS fade/ride switch. Trend-following = animal séparé (HYPE-trend winner, autre échelle).
- **HYPE exclu** (momentum), **chop exclu** (fade perd sans trend à fader).
- **LEÇON** : NE PAS laisser le Binance reconstruit (de-bounce) RÉFUTER un fade — il ne peut que CONFIRMER (artefact contre le fade, pire au hold court). A failli tuer à tort le hold-court (corrigé par cross-week clean).

## CAVEATS DURS

1. **Robustesse = PORTEFEUILLE, pas par-paire.** Seul SOL propre des 2 côtés ; les autres ont 1 côté fragile. Déployer diversifié sur les majors, jamais 1 paire isolée. ET le portefeuille n'a que 2 semaines → la diversification "sauve" sur n=2, faible puissance.
2. **TOUT est dans mai** (W507+W516). Cross-semaine ≠ cross-période → **11 juin = juge décisif.**
3. **SHORT-GAMMA** : +EV mais skew négatif (queue). Sizing prudent, zéro levier. Tail à stresser avant sizing.
4. **n petit par cellule** (12-71) → magnitude bruitée ; c'est la consistance de signe qui porte.

## AVANT DÉPLOIEMENT (TODO, post-11-juin)

- **✅ Sizing/corrélation FAIT (`_fade_sizing_book_hl.py`, in-sample)** : single-trade mean +9.6/std44/**worst −237** (ETH le pire ; BTC/SOL/XRP −70 à −92) ; **concurrence jusqu'à 12 positions simultanées = LA contrainte (pas le maxDD)** ; **corrélation SOL-XRP +0.70** (fadent ensemble), ratio diversif 0.64 (modéré) ; livre maxDD 414bps/unité, +169bps/j/unité. **SIZING SÛR = 0.25% risk/trade → 7% notionnel/trade, expo pic 84% (SANS levier), ≈ +0.12%/jour ≈ ~2.5-3.5%/MOIS in-sample**, maxDD réalisé 0.3% capital. ⚠️ queue corrélée théorique (12 positions au worst ensemble) ≈ **3% capital** ; futur-worst peut dépasser −237 (vrai crash) → 0.25%/trade conservateur + **caper la concurrence**. **= LOIN du 1%/jour rêvé ; le petit sizing (imposé par short-gamma + no-stop + corrélation) est POURQUOI. Modeste, survivable, no-levier — un vrai edge fin, pas une fortune.**
- **✅✅ SIZING LIVRE PROPRE (BTC/SOL/XRP sans ETH, `_fade_sizing_3maj_hl.py`)** : worst −92 (pas −237), maxDD 225bps, concurrence max 8, 13 trades/j, mean +12.2. **No-levier (expo≤100%, ~0.17%risk, 12.5% notionnel) ≈ +0.2%/jour ≈ +4%/MOIS** ; à 0.25%risk (18% notionnel, levier 1.45×) **+0.287%/j ≈ +5.7%/MOIS**, maxDD 0.4% capital. ⟹ **drop ETH ~DOUBLE le rendement sûr (~4-6%/mois vs ~2.4% avec ETH). ~50-90%/an in-sample ; même décoté ÷2 en OOS (~25-45%/an), ça BAT HLP (~11%/an) largement. ⟹ SI l'edge survit au 11 juin, ce bot vaut mieux que le HLP passif = l'enjeu réel.** Caveats : in-sample/mai (maxDD gonflera OOS), worst-case 1.5×−92 (crash peut dépasser), cluster corrélé SOL-XRP (8 pos × −92 ≈ −1.3% simultané, survivable), expo>100% à 0.25%=lever léger/caper concurrence. 11 juin = seul juge.
- Stress tail short-gamma (worst-trade/cluster/max-DD) → taille par trade, risque de ruine.
- Recalibration bi-hebdo des cutoffs terciles drift_4h + médiane accel-1h.
- (option) seuil de jambe 20-30bps pour + de fréquence (à re-valider carnet+OOS).

## SCRIPTS
`_oscillation_capture.py` · `_oscillation_decompose.py` · `_oscillation_hl_robust.py` · `_oscillation_oos_robust.py` · `_fade_exit_sim.py` · `_fade_maker_fill_hl.py` · `_fade_tail_stop_hl.py` · `_fade_exit_time_hl.py`/`_binance.py` · `_fade_legsize_sweep_hl.py` · `_fade_crossweek_hl.py` · `_fade_maker_fill_short_hl.py` · `_fade_bull_conditioning_hl.py` · `_fade_bull_accel_crossweek_hl.py` · `_fade_unified_sharp_hl.py` · `_fade_unified_validate_hl.py` · `_ride_continuation_hl.py`.

## VISION / ARCHITECTURE PORTEFEUILLE
**Tout-terrain = le FADE unifié (mean-reversion bull+bear sur majors mean-reverting).** Le **HYPE-trend winner** (momentum, autre échelle/construction) est la stratégie COMPLÉMENTAIRE séparée pour l'actif-momentum. Les deux = le "bot 2-stratégies" (tâche #43). Juge commun = 11 juin (cross-période, true-book).
- **MOMENTUM LONG-SCALE TESTÉ sur les 5 paires (`_momentum_majors_hl.py`, 2026-05-30 — vrai instrument : entrée trend-aligné drift_1h+drift_4h, |drift_4h|≥100, exit trailing arm50/trail30, hold 8h, taker RT6)** : **HYPE MARCHE** (TREND > NULL les 2 sem : +16.1/+8.6 au-dessus du null, win 72-76%, total +403/+340) ; **MAJORS ÉCHOUENT** (BTC/SOL/XRP négatifs les 2 sem ET **pires que NULL** : TREND−NULL −11 à −25 ; ETH idem). ⟹ **le momentum est ASSET-CHARACTER-spécifique, PAS une stratégie majors.** Majors = mean-reverting (fade) à TOUTES échelles testées ; HYPE = momentum. **Réponse au souhait user "momentum sur autres paires" : NON sur les majors établis (mesuré, clean, cross-week, null-ctrl) — MAIS le PORTEFEUILLE touche tout (fade majors + momentum HYPE).** 🔴🔴 **MAIS — MOMENTUM = BÉTA PAS ALPHA, sleeve MORTE (`_momentum_beta_check_hl.py`, 2026-05-30, scepticisme user)** : comparé au BUY-AND-HOLD (pas au null qui ne contrôle pas le béta), le **buy-hold ÉCRASE le timing sur HYPE** (W516 hold +2419bps vs momentum +340 = capté 6× moins ; les SHORTS ne rapportent rien) → le "HYPE-trend winner" n'est PAS de l'alpha de timing, c'est une **mauvaise capture du BÉTA** (pire que tenir HYPE). Les +9.77%/j du user = béta **imprévisible/non-répétable** (quel sera le prochain HYPE ?). **AUCUN alpha momentum dans les données ⟹ le bot = UNE stratégie : le FADE. Sleeve momentum ABANDONNÉE. PAS de "collecte de tokens momentum" (rien à timer ex-ante).** ⚠️⚠️ **CORRECTION MAJEURE (user, 2026-05-30) — le caractère MR/momentum N'EST PAS structurel/fixe ; c'est un RÉGIME TIME-VARYING piloté par l'INFORMATION** (j'avais re-fait l'erreur "période=propriété"). HYPE a trendé car catalyseur mai (40→66) ; juin peut le rendre choppy (le fade marcherait sur HYPE) ; les majors choppy FAUTE d'info, mais trenderont quand un catalyseur frappe. Principe microstructure : **info→trend** (fader = se faire écraser par le flux informé) / **pas d'info→mean-revert** (fader le bruit = profit, on est LP du bruit). ⟹ archi correcte = **détecteur de régime PAR ACTIF, TIME-VARYING** (fade quand no-info/chop ; OFF ou momentum quand info/trend), TOUTES les paires, les deux modes. **La queue short-gamma du fade = EXACTEMENT se faire prendre à fader un trend INFORMÉ** (ton point = la queue, une seule chose). HARD PART (non résolu) : (1) le switch de régime était break-even même avec oracle PARFAIT (`_regime_upperbound`) ; (2) distinguer info-trend vs noise-trend en temps réel = le MÊME problème de prédiction de flux qui a résisté tout le projet (TFI/OFI/exhaustion morts). **FRONTIÈRE = filtre de régime sur le fade : couper le fade quand un régime info/trend est détecté → tuer la queue. À TESTER.**
- **✅✅ FILTRE DE RÉGIME — TESTÉ & ÇA MARCHE (`_fade_regime_filter_hl.py`, recherche-informé, 2026-05-30)** : 3 agents web → seul prédicteur robuste de continuation = **FLUX SIGNÉ SOUTENU (Ahern ; makers sous-réagissent)** ; VPIN=réactif inutile ; **régime = throttle de RISQUE pas alpha**. Implémenté en kill-switch causal : **SKIP le fade si `sign(tfi_5min)==jambe & |tfi_5min|≥Q70`** (le flux taker soutient la jambe = continuation informée). **MAJORS poolés : maxDD 1159→477 (−59%), worst10 −1117→−338 (−70%), net/trade +9.1→+9.5 (PRÉSERVÉ), 71% gardés, total/maxDD 2.76→4.93 (~×1.8).** SOL spectaculaire (net +9.1→+13.2, maxDD 316→127) ; BTC +13→+15.7 ; ETH queue −50% coût EV modeste ; XRP léger coût EV. Filtres vol-expansion/surge plus faibles ; combiner sur-filtre. ⟹ **1ER signal de flux du projet à AJOUTER de la valeur (car utilisé en filtre-de-toxicité sur strat déjà rentable, PAS en alpha — prescription lit. exacte). RÉSOUT le caveat short-gamma : queue coupée ~60% à EV préservée → fade nettement plus DÉPLOYABLE.** Caveats : in-sample, Q70 fit, worst1-single PAS coupé, XRP faible. = la version minimale de l'archi time-varying du user, qui FONCTIONNE.
- **🔬 PREUVE que les catastrophes = ÉVÉNEMENTS DE MARCHÉ informés (`_eth_toxicity_proof_hl.py`, 2026-05-30, question user)** : les 6 pires trades fade ETH coïncident TOUS avec **BTC/SOL/XRP qui bougent dans le même sens à la même minute** (le −237 = BTC−92/ETH−235/SOL−131/XRP−96, market-wide selloff ; 4 des 6 au même instant 17mai 23:40). ⟹ catastrophes = info macro/marché, PAS bruit (prouvé, quantifiable). **MAIS le flux PAR-ACTIF rate les pires** : le −237 & −114 avaient flux/vol ETH BAS (tfi p54/p16, vol p3/p15 = calme avant la tempête, move soudain) → FLOW filter coupe 3/6 seulement. **Le signal universel = CROSS-ASSET (marché entier bouge ensemble).** ⟹ **MORCEAU MANQUANT = kill-switch CROSS-ASSET/MARCHÉ-WIDE** (BTC/marché bouge fort → couper TOUS les fades) : attrape ce que le flux par-actif rate + vise le tail dominant (1 événement marché → N pertes simultanées = la concurrence-12/cluster corrélé). Awareness défensive = 2 détecteurs : (a) FLUX par-actif (Ahern), (b) MARCHÉ-WIDE cross-asset. Les 2 backtestables, causaux, SANS news (le prix cross-asset suffit pour la défense ; news/social = offense seulement, dur/non-backtestable). À TESTER : kill-switch cross-asset.
- **🔴 KILL-SWITCH CROSS-ASSET TESTÉ = NE MARCHE PAS (`_fade_marketwide_exit_hl.py`, 2026-05-30)** : sortir le fade quand BTC court contre la jambe (mon hypothèse : stop "marché" informé > stop "actif"). RÉFUTÉ : POOL net +6.8→+3.4 (mkt30, **EV ~½**), worst −237→−153 (capé partiel), maxDD pas amélioré, **22% déclenchements** (trop fréquent). SOL ABÎMÉ (worst −70→−153, sortir verrouille son overshoot récupérable). ⟹ **même défaut que le stop par-actif : se déclenche trop, verrouille du récupérable.** **CONCLUSION FERME : on ne STOPPE pas la queue du fade (ni par-actif ni cross-asset), on la SIZE.** Seule awareness qui PAIE = le filtre FLUX (skip entrées toxiques avant, EV-préservé) ; il rate la catastrophe rare market-wide (−237) → ACCEPTÉE, sizée petit. Seul levier restant pour la catastrophe rare = NEWS (anticiper l'event macro invisible dans flux/prix avant) = spéculatif, non-backtestable now. Plafond mesuré de l'intelligence défensive : éviter (FLUX) oui, se sortir profitablement d'un crash (stop) non.
- **🟡 PAUSE-RÉGIME sur ENTRÉES (idée user "couper tout + re-rentrer quand calme", `_fade_market_regime_gate_hl.py`, 2026-05-30) — MARCHE pour la queue mais c'est de l'ASSURANCE chère** : distinction user juste (pause d'ENTRÉE ≠ stop mid-trade). Gate = skip entrée si \|BTC drift 15min\|≥THR (marché directionnel). **gate30 COUPE la queue (POOL worst −237→−82, maxDD 294→197)** — le stop n'y arrivait pas ; lead-in réel (BTC avait bougé 30-50bps avant l'entrée du −237). MAIS premium lourd : skip 55% des trades, EV +7.9→+4.1. Asymétrie : ETH (paire-catastrophe) net +1.1→+4.8 (gagne) ; BTC/SOL/XRP (calmes) abîmés (BTC +12.8→+2.3). gate50/75 préservent EV mais ratent le −237. ⟹ **assurance : prime (≈½ EV) payée sur TOUTES les paires pour protéger celle qui crash (imprévisible). DIAL de risque, pas free lunch.** FLUX reste l'assurance la moins chère (keye toxicité-flux, EV-préservée) ; pause market-wide = assurance-catastrophe OPTIONNELLE selon appétit risque + fréquence crashs (inconnue sur 2 sem). **Couche awareness explorée à sa limite : FLUX (cheap, garder) + pause-marché (dial optionnel) ; stop (jeter).**
- **🔴 PAUSE CO-MOVE SYSTÉMIQUE TESTÉE = ÉCHEC DÉFINITIF (`_fade_breadth_gate_hl.py`, 2026-05-30, version corrigée critique user)** : gate = pause si ≥K majors bougent ensemble (vrai signal d'event systémique vs mon BTC-isolé). 2 découvertes qui tuent l'idée : (1) **le co-move N'EST PAS RARE — c'est la NORME** : ≥3 majors alignés arrive **25-63% du temps** (majors crypto fortement corrélés, tous béta-BTC) → gater dessus saute 25-63% des trades → tue l'EV (+7.9→+2.5-5.8), comme le BTC-gate ; (2) **worst1 reste −237 dans TOUTES les configs** → le co-move démarre APRÈS l'entrée (calme-avant-tempête confirmé). ⟹ **CLÔT la détection : les pires catastrophes = chocs systémiques SOUDAINS depuis le calme, SANS lead-in à l'entrée, IRRÉDUCTIBLES par aucun gate d'entrée** (ni flux par-actif, ni BTC, ni breadth). Seuls outils : FLUX (attrape les catastrophes AVEC lead-in flux, 3/6, garder) ; les chocs soudains = encaissés/sizés ; NEWS = seul anticipateur possible (non-backtestable). Leçon : les majors crypto co-bougent par défaut (corrélation béta) → "tout le marché bouge" n'est pas un signal rare.
- **🔬 VRAI calculateur toxicité (OFI depth-norm CKS + amincissement carnet) TESTÉ vs mon raccourci tfi (`_fade_proper_toxicity_hl.py`, 2026-05-30, demande user "tu utilises le vrai détecteur ?")** : honnêtement NON je l'utilisais pas — testé sur demande. **Résultat : le vrai outil est PIRE.** Filtre OFI-depth-norm TUE l'EV (POOL net +8.5→+0.4) vs tfi qui la PRÉSERVE (+8.6) et coupe mieux la queue (worst −238→−186). RAISON (= ce que la recherche disait) : **l'OFI est CONTEMPORAIN pas prédictif** — à une fin de jambe il est TOUJOURS élevé dans le sens de la jambe (un move vient de se produire) → se déclenche sur ~tous les fades → zéro discrimination. **Mon `tfi_5min` (flux taker soutenu 5min) est en fait plus proche du SEUL survivant (persistance signée d'Ahern) que l'OFI** → raccourci heureux. **Et le −237 reste INVISIBLE même au vrai outil** (OFI p37, thinning p16, spread p64 = carnet calme avant le choc) → confirme : choc exogène SANS précurseur microstructure. ⟹ garder tfi comme filtre FLUX ; catastrophe rare = microstructure-invisible → size/news. (Reste non-testé proprement : autocorrélation/run-length de flux pure — mais tfi_5min en est déjà un proxy raisonnable.)
- **🟢🟢🟢 RENVERSEMENT — LE CROSS-VENUE BINANCE-LEAD EST LE VRAI MISSING PIECE (`_crossvenue_lead_catastrophe.py`, 2026-05-30, user a refusé mon "indétectable" + m'a renvoyé à MA recherche)** : j'avais conclu "catastrophe indétectable à l'entrée / sans précurseur microstructure" — **FAUX.** J'avais testé tous les détecteurs d'ÉTAT single-venue (tfi/OFI/thinning/breadth = contemporains) mais JAMAIS les LEADING que ma propre recherche flaggait (CUSUM/BOCPD transition + **cross-venue Binance lead = "le lead causal le plus fort"**). Test : **aux 6/6 pires catastrophes HL ETH, Binance bougeait DÉJÀ dans le sens de la jambe 1-2min AVANT l'entrée HL** (le −237 : Binance ETH −59bps sur [T−120s,T] pendant que HL tfi=p54 calme). **Le calme HL = HL LAGGE Binance** (= ma trouvaille "Binance lead HL" ignorée). Filtre Binance-lead sur HL ETH fades : **AMÉLIORE l'EV (+2.1→+6.6/+9.6) ET coupe la queue (worst −238→−71/−101, maxDD 1604→421/694)** — l'OPPOSÉ des filtres single-venue (qui tuaient l'EV), car VRAI LEAD pas contemporain. **⟹ la queue short-gamma (seul vrai défaut du fade) est potentiellement RÉDUCTIBLE en améliorant l'EV.** Breadth échouait car interne-HL (co-move=norme, post-entrée) ; Binance = venue DIFFÉRENTE qui mène. Caveats : ETH-only/6 events/in-sample/seuils fittés ; **Binance=SIGNAL only (exec interdite MiCA)** ; latence OK (1-2min lead). **À VALIDER : filtre Binance-lead sur le LIVRE COMPLET (toutes paires) + OOS 11 juin.** ⚠️ LEÇON (énième) : j'ai déclaré "impossible" sans tester les outils que MA PROPRE recherche désignait comme les bons — ne JAMAIS conclure "indétectable" sans avoir testé les signaux leading/cross-venue.
- **🟢🟢🟢 VALIDATION LIVRE COMPLET — le filtre Binance-lead GÉNÉRALISE (`_fade_binlead_fullbook_hl.py`, 2026-05-30)** : POOL (BTC/SOL/XRP/ETH, fade+FLUX) **net +7.9→+11.2, worst −238→−71, maxDD 448→200** ; **cross-week : W507 +10.1→+13-14 & W516 +6.1→+9** (les 2 semaines net↑ + queue↓) ; chaque paire améliore ou tient (XRP +4.5→+9.2 maxDD 193→67 ; SOL +13.4→+18.7 ; ETH binBTC +4.3 worst −238→−101 = ETH REDEVIENT tradeable). binSAME garde 61%, binBTC garde 77%. ⟹ **la queue short-gamma N'EST PLUS "irréductible/size-for-it" — elle est RÉDUCTIBLE et la réduire AMÉLIORE l'EV.** Mécanisme : skip les fades où Binance court déjà dir-jambe = enlève les toxiques (continuation cross-venue confirmée) SANS toucher les bons (leg = bruit HL idiosyncratique). **BOT DÉPLOYABLE MIS À JOUR : fade (extension régime) + FLUX + BINANCE-LEAD (skip si \|drift Binance 120s\|≥30 & sign==jambe) + sizing.** Caveats : in-sample 2 sem, seuils 30bps/120s fittés, mid Binance recon, **Binance=SIGNAL-only (MiCA → feed temps-réel requis live, latence OK car lead 1-2min)**, OOS 11 juin à confirmer.
- **↳ CROSS-WEEK + STOP (`_fade_filter_validate_hl.py`, 2026-05-30)** : (1) **FLOW = ASSURANCE W516-driven** : W516 (semaine toxique) maxDD 1144→375 (−67%) & net +5.2→+7.6 ; W507 (calme) petit coût (net +14.5→+12.5, pas de queue à couper) → coupe la queue QUAND toxicité, coût modeste sinon. Sensé mais n=2 sem mince, pas un gain uniforme. (2) 🔴 **STOP DUR = CONTRE-PRODUCTIF (corrige mon prior −50/−60 qui était FAUX)** : caper worst1 (−237→−56) via SL **AUGMENTE le maxDD** (416 sans stop → 632-814 avec) ET coupe l'EV (+9.7→+4-6) — le stop convertit des dips récupérables en pertes clustered (problème MR, déjà vu mur#2 : SL−20 rendait SOL nég). ⟹ **PAS de stop dur ; SIZER PETIT pour encaisser la queue −237** (short-gamma : on ne se stoppe pas, on dimensionne). **CONFIG DÉPLOYABLE = fade unifié + filtre FLOW + AUCUN stop + sizing petit.** SOL excellent (net +8.9→+13.4, maxDD 317→124), BTC bon ; ETH/XRP marginaux (FLOW coûte un peu d'EV).
